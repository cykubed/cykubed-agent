apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: "node-{{node_cache_key}}"
  namespace: "{{namespace}}"
spec:
  volumeSnapshotClassName: cykubed-snapshotclass
  source:
    persistentVolumeClaimName: "node-{{node_cache_key}}"


