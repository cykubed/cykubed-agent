apiVersion: batch/v1
kind: Job
metadata:
    labels:
        branch: master
        cykubed-job: runner
        local_id: '1'
        project_id: '10'
        testrun_id: '20'
    name: cykubed-runner-project-20
    namespace: cykubed
spec:
    activeDeadlineSeconds: 3600
    backoffLimit: 10
    parallelism: 1
    template:
        metadata:
            labels:
                branch: master
                local_id: '1'
                project_id: '10'
                testrun_id: '20'
        spec:
            containers:
            -   args:
                - run
                - '20'
                env:
                -   name: BUILD_DIR
                    value: /build
                -   name: NODE_CACHE_DIR
                    value: /node
                -   name: CYPRESS_RETRIES
                    value: ''
                -   name: TZ
                    value: UTC
                -   name: REDIS_PASSWORD
                    valueFrom:
                        secretKeyRef:
                            key: redis-password
                            name: cykubed-redis
                envFrom:
                -   configMapRef:
                        name: cykubed-agent-configmap
                -   secretRef:
                        name: cykubed-agent-secrets
                image: cykubed-runner:1234
                name: cykubed-runner
                resources:
                    limits:
                        cpu: '2.0'
                        memory: 4.0G
                    requests:
                        cpu: '2.0'
                        memory: 4.0G
                volumeMounts:
                -   mountPath: /build
                    name: build-volume
                -   mountPath: /node
                    name: node-volume
            priorityClassName: cykubed-low-priority
            restartPolicy: OnFailure
            securityContext:
                fsGroup: 10000
                runAsGroup: 10000
                runAsNonRoot: true
                runAsUser: 10000
            serviceAccountName: cykubed
            volumes:
            -   name: build-volume
                persistentVolumeClaim:
                    claimName: build-ro-pvc-deadbeef0101
                    readOnly: true
            -   name: node-volume
                persistentVolumeClaim:
                    claimName: node-ro-pvc-absd234weefw
                    readOnly: true
    ttlSecondsAfterFinished: 3600
